// TODO: Will be reverted after Elfin release
// import { VPDApiClient } from '../../../config/api-client';

// const {
//   get, validateResponse,
// } = VPDApiClient;

// export const getEmailList = (taxableSubjectId) => get(`/vpd-api/v1.0/organization-units/${taxableSubjectId}/user-list`)
//   .then(validateResponse);
